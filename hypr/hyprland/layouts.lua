--- Dwindle layout config ---
hl.config({
	dwindle = {
		preserve_split = true,
	},
})

--- Master layout config ---
hl.config({
	master = {
		new_status = "master",
		drop_at_cursor = true,
	},
})

--- Scrolling layout config ---
hl.config({
	scrolling = {
		fullscreen_on_one_column = true,
		focus_fit_method = 0,
		column_width = 0.9,
	},
})
